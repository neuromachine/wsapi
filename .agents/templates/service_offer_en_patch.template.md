# Service Offer EN Patch Input

## Target file

```text
storage/app/blocks/items/<categoryKey>.json
```

## Source category context

```text
Category key:
RU title:
RU description:
Current package count:
Business segment:
```

## Requirements

- Preserve root `properties`.
- Add or improve `en.properties`.
- Preserve the same package count unless explicitly approved.
- Keep package progression logical.
- Use concise business English.
- Leave `vi.properties` empty if Vietnamese content is not requested.
